This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev

порядок дій
1. телега не публикует фото
2. инста не публикует, почему при подключении не справивает логин/пароль
3. posts/фйди нет страницы (нельзя посмотреть пост и чернетку)

структура

├── postbridge/
│   ├── app/
│   │   ├── layout.tsx          ← є (змінимо)
│   │   ├── page.tsx            ← є (замінимо на редирект)
│   │   ├── auth/
│   │   │   └── page.tsx        ← сторінка логін/реєстрація
│   │   ├── dashboard/
│   │   │   └── page.tsx        ← головний дашборд
│   │   ├── platforms/
│   │   │   └── page.tsx        ← підключення платформ
│   │   ├── posts/
│   │   │   ├── page.tsx        ← список постів
│   │   │   └── new/
│   │   │       └── page.tsx    ← форма нового поста
│   │   └── api/
│   │       ├── publish/
│   │       │   └── route.ts    ← POST: публікує на платформи
│   │       └── platforms/
│   │           └── route.ts    ← GET/POST: управління платформами
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Sidebar.tsx
│   │   │   └── AppShell.tsx
│   │   ├── platforms/
│   │   │   ├── PlatformCard.tsx
│   │   │   └── modals/
│   │   │       ├── InstagramModal.tsx
│   │   │       ├── TelegramModal.tsx
│   │   │       ├── PromModal.tsx
│   │   │       └── WooModal.tsx
│   │   ├── posts/
│   │   │   ├── PostForm.tsx
│   │   │   ├── PostList.tsx
│   │   │   └── ValidationPanel.tsx
│   │   └── ui/
│   │       ├── Button.tsx
│   │       ├── Input.tsx
│   │       ├── Modal.tsx
│   │       └── Toast.tsx
│   └── lib/
│       ├── supabase/
│       │   ├── client.ts       ← браузерний клієнт
│       │   └── server.ts       ← серверний клієнт (для route handlers)
│       └── platforms/
│           ├── telegram.ts     ← логіка відправки в TG
│           ├── instagram.ts    ← логіка Meta API
│           ├── prom.ts         ← логіка Prom API
│           └── woocommerce.ts  ← логіка WooCommerce API